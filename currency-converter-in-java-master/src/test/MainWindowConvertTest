class MainWindowConterterTest{

    @test 
    void ValideTest(){
        MainWindow conversion = new MainWindow();
        conversion.convert(CAD,USD, ArrayList<Currency>,50); 
    }

    @test 
    void InvalideTest(){
        MainWindow conversion = new MainWindow();
        conversion.convert(ABC,DEF,ArrayList<Currency>,50); 
    }
}